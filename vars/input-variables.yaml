variables:
  isMain: $[in(variables['Build.SourceBranch'], 'refs/heads/main', 'refs/heads/master')]
  isTriggered: $[in(variables['Build.Reason'], 'BatchedCI', 'IndividualCI', 'PullRequest', 'Schedule')]