variables:
  runSnapshotTestsEnabled: ${{ or(eq(parameters.runSnapshotTests, true), eq(parameters.runSnapshotTests, 'true')) }}
  runUnitTestsEnabled: ${{ or(eq(parameters.runUnitTests, true), eq(parameters.runUnitTests, 'true')) }}
  chartTypeModified: false
