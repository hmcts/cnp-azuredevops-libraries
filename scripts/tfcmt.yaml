ci:
  owner:
  - type: envsubst
    value: hmcts
  repo:
  - type: envsubst
    value: sds-jenkins-infrastructure
terraform:
  plan:
    template: |
      ## Plan Result
      {{if .Result}}
      <pre><code>{{ .Result }}
      </pre></code>
      {{end}}
      <summary>[See plan in Azure DevOps](https://dev.azure.com//hmcts/CNP/_build/results?buildId={0}&view=charleszipp.azure-pipelines-tasks-terraform.azure-pipelines-tasks-terraform-plan)</summary>