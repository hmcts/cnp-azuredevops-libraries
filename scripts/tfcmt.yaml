ci:
  vars:
    - ChangedResult: |
{{if and .ChangedResult (eq .Sensitive false) }}
<details><summary>Change Result (Click me)</summary>
{{ wrapCode .ChangedResult }}
</details>
{{ else }}
<a href="https://dev.azure.com/$ORGANISATION/$PROJECT/_build/results?buildId=$BUILD_ID&view=charleszipp.azure-pipelines-tasks-terraform.azure-pipelines-tasks-terraform-plan">See plan in Azure DevOps</a>
{{end}}