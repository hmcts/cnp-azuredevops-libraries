ci:
  owner: "hirosassa"
  repo: "tfcmt-gitlab"
terraform:
  use_raw_output: true
  plan:
    template: |
      ## {{if eq .ExitCode 1}}:x: {{end}}Plan Result - ${{ parameters.environment }}-${{ parameters.component }} {{if .Vars.target}} ({{.Vars.target}}){{end}}