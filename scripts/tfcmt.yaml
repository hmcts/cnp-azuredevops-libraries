ci:
  owner:
  - type: envsubst
    value: hmcts
  repo:
  - type: envsubst
    value: sds-jenkins-infrastructure
terraform:
  plan:
    template: |
      ## Plan Result
      {{if .Result}}
      <pre><code>{{ .Result }}
      </pre></code>
      {{end}}
      <summary>See ADO for Plan.</summary>